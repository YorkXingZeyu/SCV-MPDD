# === Core deep-learning stack ===
# Install torch/torchvision matching YOUR CUDA version first (see README);
# the versions below are what this project was tested with (CUDA 12.1).
torch==2.1.2
torchvision==0.16.2

# === Diffusion / fine-tuning ===
diffusers==0.20.1        # Stable Diffusion 2 inpainting pipeline
transformers==4.36.0     # CLIP text encoder / tokenizer
accelerate==0.23.0       # training launcher used by train.sh
peft==0.5.0              # LoRA adapters
huggingface_hub==0.19.4  # keep cached_download API required by diffusers 0.20.1

# === Evaluation & image ops ===
opencv-python==4.9.0.80  # Gaussian blur, color ops; avoid untested OpenCV 5.x
scikit-learn>=1.3        # roc_auc_score
numpy==1.26.4            # PyTorch 2.1-compatible NumPy 1.x
Pillow>=10.0             # image I/O

# Note: bitsandbytes / xformers / wandb are OPTIONAL and NOT required by the
# default scripts. Only install them if you enable the corresponding flags in
# train_realfill.py (--use_8bit_adam / --enable_xformers / --report_to wandb).
